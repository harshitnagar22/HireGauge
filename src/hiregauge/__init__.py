"""HireGauge — specialized, multi-agent candidate self-assessment.

Public surface is intentionally small; import submodules directly (e.g.
``from hiregauge.models import CandidateProfile``) to avoid pulling heavy optional
dependencies at import time.
"""

__version__ = "0.1.0"
