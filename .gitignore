# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenvs
.venv/
venv/
env/

# Env / secrets
.env
.env.*
!.env.example

# HireGauge runtime
# Glob covers .hiregauge_cache/, the legacy .hireme_cache/, and any future *_cache dir.
# Cache files hold fetched candidate data (PII) and must never be committed — see #16.
*_cache/
reports/
*.report.md
*.report.json
*.report.html

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
report.md

# Local-only: build-log / planning notes (docs/rubrics.md IS published)
docs/PLAN.md
docs/TODO.md

# Candidate inputs (resumes / exports) — never commit real PII
resume/
